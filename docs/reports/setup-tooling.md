# レポート: Lint/Format/型設定 (`setup-tooling`)

## 実施概要

- Python プロジェクト向けに Lint / Formatter / 型チェックの設定を整備した。
- `pyproject.toml` と `requirements.txt` に必要なツールを追加し、基本的な設定を行った。

## メモ

- 追加のルール調整が必要になった場合は、`pyproject.toml` 側で行うこと。
